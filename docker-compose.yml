services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/data
    environment:
      - RUST_LOG=debug
      - RUST_BACKTRACE=1
      - UID
      - GID
    user: "${UID}:${GID}"
    restart: unless-stopped
