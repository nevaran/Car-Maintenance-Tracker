services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - RUST_LOG=debug
      - RUST_BACKTRACE=1
    user: "${UID:-1000}:${GID:-1000}"
    restart: unless-stopped
